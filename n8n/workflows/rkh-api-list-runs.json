{
  "name": "RKH - API - List Runs",
  "nodes": [
    {
      "id": "list-runs-webhook",
      "name": "List Runs Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        260,
        300
      ],
      "parameters": {
        "httpMethod": "GET",
        "path": "rkh-api-list-runs",
        "responseMode": "lastNode",
        "options": {}
      }
    },
    {
      "id": "read-persistent-assurance-data",
      "name": "Read Persistent Assurance Data",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        520,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs'); const path = require('path');\nconst root = path.join(process.env.RKH_ASSURANCE_DATA_DIR || path.join(process.cwd(), 'rkh-assurance-data'), 'runs');\nconst runs = fs.existsSync(root) ? fs.readdirSync(root, { withFileTypes: true }).filter(entry => entry.isDirectory()).map(entry => { try { return JSON.parse(fs.readFileSync(path.join(root, entry.name, 'run.json'), 'utf8')); } catch { return null; } }).filter(Boolean).sort((a, b) => String(b.created_at).localeCompare(String(a.created_at))) : [];\nreturn [{ json: { runs } }];"
      },
      "notes": "Reads only the configured persistent RKH_ASSURANCE_DATA_DIR. Never exposes raw filesystem paths to the browser.",
      "notesInFlow": true
    }
  ],
  "connections": {
    "List Runs Webhook": {
      "main": [
        [
          {
            "node": "Read Persistent Assurance Data",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "rkh-rkh-api-list-runs",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "pinData": {},
  "tags": []
}
