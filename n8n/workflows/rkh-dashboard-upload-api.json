{
  "name": "RKH - Dashboard Upload API",
  "nodes": [
    {
      "id": "dashboard-upload-webhook",
      "name": "Dashboard Upload Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        260,
        300
      ],
      "parameters": {
        "httpMethod": "POST",
        "path": "rkh-dashboard-upload",
        "responseMode": "responseNode",
        "options": {
          "rawBody": false
        }
      },
      "notes": "POST multipart/form-data with the file in binary property data.",
      "notesInFlow": true
    },
    {
      "id": "validate-persist-and-create-run",
      "name": "Validate, Persist, and Create Run",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst path = require('path');\nconst item = $input.first();\nconst binary = item.binary && item.binary.data;\nif (!binary || !binary.data) throw new Error('UPLOAD_BINARY_MISSING');\nconst filename = String(binary.fileName || item.json.body?.filename || 'upload').split(/[\\/]/).pop();\nconst extension = filename.toLowerCase().split('.').pop();\nif (!['csv', 'xlsx', 'xls'].includes(extension)) throw new Error('UNSUPPORTED_FILE_TYPE');\nconst maxBytes = Number(process.env.RKH_MAX_UPLOAD_BYTES || 25000000);\nconst sourceBuffer = Buffer.from(binary.data, 'base64');\nif (sourceBuffer.length > maxBytes) throw new Error('UPLOAD_TOO_LARGE');\nconst now = new Date();\nconst day = now.toISOString().slice(0, 10).replaceAll('-', '');\nconst stamp = day + '-' + now.toISOString().slice(11, 19).replaceAll(':', '');\nconst runId = 'RKH-' + day + '-' + stamp + '-' + Math.random().toString(36).slice(2, 7).toUpperCase();\nconst dataRoot = process.env.RKH_ASSURANCE_DATA_DIR || path.join(process.cwd(), 'rkh-assurance-data');\nconst runDir = path.join(dataRoot, 'runs', runId);\nfs.mkdirSync(runDir, { recursive: true });\nconst sourcePath = path.join(runDir, filename.replace(/[^a-zA-Z0-9._-]/g, '_'));\nconst runPath = path.join(runDir, 'run.json');\nconst run = { run_id: runId, filename, source: 'DASHBOARD_UPLOAD', created_at: now.toISOString(), updated_at: now.toISOString(), status: 'RECEIVED', total_rows: 0, eligible_count: 0, excluded_count: 0, green_count: 0, yellow_count: 0, amber_count: 0, red_count: 0, source_path: sourcePath, results_json_path: path.join(runDir, 'results.json'), results_csv_path: path.join(runDir, 'results.csv'), mapping_confidence: 0, rule_version: 'MAXIMO-MMS-KEYWORD-v1', error_code: '', error_message: '' };\nfs.writeFileSync(sourcePath, sourceBuffer);\nfs.writeFileSync(runPath, JSON.stringify(run, null, 2));\nreturn [{ json: { ...run, response: { run_id: runId, status: 'RECEIVED' } } }];"
      },
      "notes": "Writes the original source and run.json before the processor starts. Configure fs in the n8n Code node allowlist.",
      "notesInFlow": true
    },
    {
      "id": "respond-with-run-id",
      "name": "Respond with Run ID",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        760,
        220
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json.response) }}",
        "options": {}
      }
    },
    {
      "id": "start-maximo-mms-processor",
      "name": "Start MAXIMO MMS Processor",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        760,
        400
      ],
      "parameters": {
        "workflowId": "REPLACE_WITH_MAXIMO_MMS_PROCESSOR_WORKFLOW_ID",
        "mode": "onceForAllItems",
        "options": {
          "waitForSubWorkflow": false
        }
      },
      "notes": "After importing both workflows, replace the placeholder workflow ID with the processor workflow ID.",
      "notesInFlow": true
    }
  ],
  "connections": {
    "Dashboard Upload Webhook": {
      "main": [
        [
          {
            "node": "Validate, Persist, and Create Run",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate, Persist, and Create Run": {
      "main": [
        [
          {
            "node": "Respond with Run ID",
            "type": "main",
            "index": 0
          },
          {
            "node": "Start MAXIMO MMS Processor",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "rkh-rkh-dashboard-upload-api",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "pinData": {},
  "tags": []
}
